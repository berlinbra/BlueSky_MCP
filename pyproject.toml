[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bluesky_mcp"
version = "0.1.0"
dependencies = [
    "mcp-sdk",
    "atproto"
]

[tool.hatch.build.targets.wheel]
packages = ["src/bluesky_mcp"]